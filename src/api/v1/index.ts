import { Router } from 'express';

const router = Router();

// API v1 routes - currently empty, only admin auth is active
// router.use('/yachts', yachtRoutes);
// router.use('/blogs', blogRoutes);

export default router;
